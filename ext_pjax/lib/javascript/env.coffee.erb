if window.Env?
  console.log "ExtPjax Overriding #{this.name}.Env"

class window.Env
  @current: '<%= Rails.env.to_s %>'
  @debug_trace: <%= ExtPjax.config.debug_trace? %>

  @initialize: =>
    for env in ['development', 'test', 'vagrant', 'staging', 'production']
      @[env] = @is(env)

  @is: (env) =>
    @current == env

window.Env.initialize()
