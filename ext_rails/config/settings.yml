types:
  mail_to: emails
  backup_dir: pathname

shared:
  default_url_options: $METHOD
  server: $METHOD
  server_ssl: true
  mail_to: admin@email.example.com
  mail_from: noreply@email.example.com
  mail_address: localhost
  mail_port: 1025
  pgstats_enabled: true
  backup_split_size: 4
  backup_dir: /opt/storage/backup

test:
  server_host: localhost
  server_port: 3000
  server_ssl: false
  mail_domain: $ALIAS server
  backup_dir: ./tmp/backup

development:
  server_host: localhost
  server_port: 3000
  server_ssl: false
  mail_domain: $ALIAS server
  backup_dir: ./tmp/backup

vagrant:
  server_host: vagrant-web.test
  mail_domain: email.vagrant-web.test

staging:

production:
